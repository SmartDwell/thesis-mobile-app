import 'package:flutter/material.dart';

/// Страница доступа
class AccessPage extends StatelessWidget {
  const AccessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AccessPage'),
      ),
    );
  }
}
